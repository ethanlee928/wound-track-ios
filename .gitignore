# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xccheckout
*.moved-aside

# Python / uv
model-export/.venv/
__pycache__/
*.pyc

# ML model artifacts — never tracked in git.
# COCO `.pt` files are auto-downloaded by ultralytics on first use.
# Fine-tuned `wound-*.pt` checkpoints live on the training server
# (scp from /home/ethanlee/wound-segmentation-yolo/runs/.../weights/best.pt).
# `.mlpackage` exports are regenerated locally with
# `uv run yolo export model=<file>.pt format=coreml imgsz=512 nms=False`.
*.pt
*.mlpackage/

# Local IDE / agent state
.claude/

# Stray `/export` chat dumps
WoundTrack/2026-*-command-message*.txt
2026-*-this-session-is-being-continued-from-a-previous-c.txt

# Local model-export outputs
model-export/exports/
.gstack/

# Training repo (wound-segmentation-yolo) — code only, no data/weights/logs
wound-segmentation-yolo/.venv/
wound-segmentation-yolo/runs/
wound-segmentation-yolo/dataset/
wound-segmentation-yolo/pressure-ulcer-cls/
wound-segmentation-yolo/pressure-ulcer-cls-clean/
wound-segmentation-yolo/pressure-ulcer-staging/
wound-segmentation-yolo/logs/
wound-segmentation-yolo/.env
wound-segmentation-yolo/*.npy
